{
  "name": "vela-web",
  "version": "0.340.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.12",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.14.0"
  },
  "overrides": {
    "postcss": "8.5.20",
    "esbuild": "0.28.1"
  },
  "scripts": {
    "dev:www": "bun run --filter @vela/www dev",
    "dev:observatory": "bun run --filter @vela/observatory dev",
    "lint": "bun run --filter @vela/observatory lint",
    "typecheck": "bun run --filter @vela/www typecheck && bun run --filter @vela/observatory typecheck",
    "check": "bun run check:brand && bun run check:bundle && bun run check:boundary && bun run typecheck",
    "check:brand": "bun run --filter @vela/brand check",
    "check:bundle": "bun run --filter @vela/frontier-data check",
    "check:boundary": "bun scripts/check-read-only-boundary.mjs",
    "test:roots": "bun run --filter @vela/frontier-data test",
    "test:manifests": "bun run --filter @vela/www test:manifest && bun run --filter @vela/observatory test:manifest",
    "test:e2e": "bun run --filter @vela/observatory test:e2e",
    "test:a11y": "bun run --filter @vela/observatory test:a11y",
    "test:visual": "bun run --filter @vela/observatory test:visual",
    "test:budgets": "bun scripts/check-budgets.mjs",
    "test": "bun test scripts/read-only-boundary.test.ts && bun run --filter @vela/brand test && bun run --filter @vela/frontier-data test && bun run --filter @vela/www test && bun run --filter @vela/observatory test",
    "build": "bun run --filter @vela/brand build && bun run --filter @vela/frontier-data check && bun run --filter @vela/www build && bun run --filter @vela/observatory build",
    "format:check": "git diff --check"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
