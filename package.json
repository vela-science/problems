{
  "name": "vela-web",
  "version": "0.433.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.12",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.14.0"
  },
  "overrides": {
    "@hono/node-server": "2.0.10",
    "postcss": "8.5.20",
    "esbuild": "0.28.1",
    "sharp": "0.35.0"
  },
  "scripts": {
    "dev:www": "bun run --filter @vela/www dev",
    "dev:observatory": "bun run --filter @vela/observatory dev",
    "lint": "bun run --filter @vela/www lint && bun run --filter @vela/observatory lint",
    "typecheck": "bun run --filter @vela/ui typecheck && bun run --filter @vela/www typecheck && bun run --filter @vela/observatory typecheck",
    "check": "bun run check:release-identity && bun run check:brand && bun run check:bundle && bun run check:boundary && bun run check:tokens && bun run check:projection && bun run check:design-system && bun run typecheck",
    "check:release-identity": "bun scripts/check-release-identity.mjs",
    "check:brand": "bun run --filter @vela/brand check",
    "check:bundle": "bun run --filter @vela/frontier-data check",
    "check:boundary": "bun scripts/check-read-only-boundary.mjs",
    "check:tokens": "bun scripts/check-token-references.mjs",
    "check:projection": "bun scripts/check-projection-compatibility.mjs",
    "install:vela:linux": "bun scripts/install-recorded-vela.mjs",
    "db:check": "bun run --filter @vela/frontier-data db:check",
    "db:migrate": "bun run --filter @vela/frontier-data db:migrate",
    "projection:snapshot": "bun run --filter @vela/frontier-data projection:snapshot",
    "projection:reconstruct": "bun run --filter @vela/frontier-data reconstruct --",
    "projection:verify": "bun run --filter @vela/frontier-data verify",
    "sources:refresh": "bun run --filter @vela/frontier-data sources:refresh",
    "sources:verify": "bun run --filter @vela/frontier-data sources:verify",
    "test:roots": "bun run --filter @vela/frontier-data test",
    "test:manifests": "bun run --filter @vela/www test:manifest && bun run --filter @vela/observatory test:manifest",
    "test:budgets": "bun scripts/check-budgets.mjs",
    "test:budgets:observatory": "VELA_BUDGET_SCOPE=observatory bun scripts/check-budgets.mjs",
    "check:design-system": "bun run --filter @vela/ui check && bun scripts/check-observatory-design-system.mjs",
    "test": "bun test scripts/check-projection-environment.test.ts scripts/check-release-identity.test.ts scripts/install-recorded-vela.test.ts scripts/read-only-boundary.test.ts scripts/projection-compatibility.test.ts scripts/token-references.test.ts scripts/vercel-should-build.test.ts && bun run --filter @vela/brand test && bun run --filter @vela/ui test && bun run --filter @vela/frontier-data test && bun run --filter @vela/www test && bun run --filter @vela/observatory test",
    "build": "bun run --filter @vela/brand build && bun run --filter @vela/frontier-data check && bun run --filter @vela/www build && bun run --filter @vela/observatory build",
    "format:check": "git diff --check",
    "docs:sync": "bun run --filter @vela/www docs:sync"
  }
}
