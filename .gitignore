.claude/
.DS_Store
*.log

# Dependencies
node_modules/

# Astro/build output
.astro/
dist/
apps/*/.astro/
apps/*/.next/
apps/*/dist/
apps/*/out/
apps/*/storybook-static/
apps/*/next-env.d.ts
*.tsbuildinfo

# Local editor and agent artifacts
.vscode/
.agents/skills/impeccable/
.codex/hooks.json
# Written by the local design skill. `.vercelignore` already excluded it, and
# nothing in the repository reads it — `preflight.json` names three components
# that no longer exist, so it was a tracked file recording a tree from before
# several deletions.
.hallmark/
.env*.local
tmp/
output/
prototypes/
.vercel
apps/www/public/.well-known/vela-web.json
apps/www/public/.well-known/vela-site.json
apps/observatory/public/.well-known/vela-site.json
apps/*/public/assets/fonts/
apps/observatory/public/favicon.svg
apps/observatory/public/data/

# CI and local reconstructions check the canonical repositories out here.
# `.github/actions/checkout-repositories` writes sources/<slug>; committing one
# would embed another repository in this one.
/sources/
