.claude/
.DS_Store
*.log

# Dependencies
node_modules/

# Astro/build output
.astro/
dist/
apps/*/.astro/
apps/*/.next/
# Development state harness output. Rendered on demand, served by nothing.
apps/*/.state-harness/
apps/*/dist/
apps/*/out/
apps/*/storybook-static/
apps/*/next-env.d.ts
*.tsbuildinfo

# Local editor and agent artifacts
.vscode/
.agents/skills/impeccable/
.codex/hooks.json
# Written by the local design skill. `.vercelignore` already excluded it, and
# nothing in the repository reads it — `preflight.json` names three components
# that no longer exist, so it was a tracked file recording a tree from before
# several deletions.
.hallmark/
.env*.local
tmp/
output/
prototypes/
.vercel
apps/problems/.generated/
apps/problems/public/.well-known/vela-site.json
apps/*/public/assets/fonts/
apps/problems/public/favicon.svg
apps/problems/public/data/

# Local development may check canonical repositories out here. The release
# command uses a private disposable directory instead; committing either would
# embed another repository in this one.
/sources/
