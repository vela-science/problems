# Vela Web v0.420.6

Vela Web 0.420.6 repairs the Observatory deployment path and one typed-relation
label without changing the rooted projection.

This release:

- rebinds the Vercel Preview and Production environments to the intended
  `observatory_projection_reader` credential on Neon's read-only compute;
- verifies that the reader retains only `SELECT` access to the `observatory`
  schema;
- renders an incoming `synthesized_from` relation as “Source for,” eliminating
  the duplicated “synthesized from from” heading; and
- adds a focused grammatical-direction regression for outgoing and incoming
  relationship headings.

The full Observatory preview builds from the same
`sha256:0f455da708e0a2fa1ed36ca9ece19db440700738cfac61b0d9c30acb481aa00a`
projection root. This release changes no Frontier, protocol, authority,
accepted scientific state, or projection row.
