# Observatory v0.360.5 release evidence

This patch completes the v0.360 workflow contract over the read-only Neon
projection.

- Exact finding routes may materialize beneath the four prebuilt frontier
  shells; unknown frontier and finding identifiers still return 404.
- Work, review, graph, and search controls create shareable browser-history
  entries. Invalid query state is normalized without adding history noise.
- Search no longer claims a fabricated independent artifact root. The client,
  API header, response body, and Neon release must agree on one exact projection
  root.
- Virtualized search retains input focus while moving through the complete
  bounded result set.
- Clean unit tests use typed in-memory fixtures. The release workflow separately
  runs live Neon projection checks and HTTP runtime route smoke tests.

No canonical frontier, protocol, authority, editorial, or accepted-state byte
changes are included.
