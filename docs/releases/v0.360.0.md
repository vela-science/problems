# Observatory v0.360.0 release evidence

## Frozen baseline

- Implementation baseline: `b4b91f4cbd9b9e2079d3645c6c8bc77df2a3d731`
- Prior release: `v0.350.1`
- Prior Observatory bundle root: `sha256:3622152093195dbbc5b996d536b91025dde1a8c54bab3ec909d61025a9240570`
- Prior bundle generator: `vela 0.911.0`
- Fresh first-party usability audit: `89/100`
- Pre-existing editorial work was confined to `apps/www` and excluded from this release.

## Vela 0.912.0 replay and projection gate

The published macOS archive matched its locked SHA-256. Its `vela 0.912.0`
binary verified the clean `origin/main` tip of each configured frontier before
the read model was written to Neon. Git commit/tree identity and every event,
snapshot, proposal, registry, artifact, packet, finding, review, and graph root
are bound into the release manifest.

The generator also consumed the retained, ignored Decision Plan evidence files
referenced by signed decision events. Those files were copied read-only into the
temporary exact checkouts; no frontier checkout or canonical byte was changed.

- Generator binary root: `sha256:5510efe75ec202f2b023de4baf72c64c3b21d6017f55d990147b651bce96c6f2`
- Current projection release root: `sha256:b9a48967e6d5c90e07d49dea460fd9594d2d9a9098c30e18fc0d642913081a44`
- Erdős source commit: `ba9c44112ecd2900c75563487ac29134be277d57`
- Erdős projection: 2,770 findings, 2,189 events, and the preserved strict-blocker classification

No generated frontier or search JSON is retained in the web repository. The
projection can be deleted and rebuilt from the bound source commits without
changing canonical frontier history.

## Release gates

The final commit, tag, deployment identity, Browser audit, production manifest,
and parent ecosystem lock are recorded here after each gate passes.
