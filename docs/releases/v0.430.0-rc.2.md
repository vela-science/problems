# Vela Web v0.430.0-rc.2

Vela Web 0.430.0-rc.2 replaces rc.1 after two exact source changes and one
projection-integrity repair. It remains a bounded, first-party Math Atlas
alpha: the four Git Frontier repositories are authoritative, Neon is a
disposable read projection, and the website cannot change scientific
Standing.

The candidate:

- binds released Vela `0.950.1` by exact platform-specific binary bytes;
- projects Formal commit `410528e49cff7fff528bd46cfc80062df5e40ea8`
  after its Erdős 835 mission was sealed;
- projects quantum commit `0de3163277031f2337206e54127f3ecab5154c09`,
  including the bounded `[[10,1,4]]`
  Submission and pending Proposal without presenting either as accepted;
- refreshes eight externally acquired source bundles, retains 14 declared
  source observations, and exposes 9,526 source-native records with 6,292
  explicit Frontier bindings;
- removes checkout-local absolute paths from projected Vela commands, so two
  clean checkouts of identical inputs produce byte-identical release roots;
- restores one main landmark, one responsive navigation landmark, explicit
  mobile dismissal, and a high-contrast focus token without forking the
  official shadcn primitive source;
- keeps source observations independent of web releases and validates them
  against their retained declarations;
- verifies the rooted 100,000-record synthetic generation benchmark, which
  establishes deterministic chunking and cursor behavior only, not Neon,
  concurrency, egress, or production-scale performance;
- keeps the application reader SELECT-only and all scientific Decisions in the
  canonical Frontier repositories; and
- retains prior projection releases so an older root-bound deployment remains
  readable during release skew.

The dry-run and activation rebuild produced the same candidate over one
immutable adapter set:

```text
release_root:            sha256:e37cb1eea2950077331b31e6c277f2ae09428a05da563d3a8e2a935757428a69
source_adapter_set_root: sha256:7d8c8a7c722bd6aef6af5080f5adf4db18b133d0e43682714ca23e8e06ecaaac
```

The read projection is active and its schema, table roots, Frontier commits,
reader permissions, and corpus counts verify exactly. Public deployment remains
a separate operation and must identify the final rc.2 commit.

This candidate changes no accepted Claim, Proposal standing, repository
authority rule, protocol schema, or editorial content.
