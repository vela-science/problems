# Observatory v0.360.4 release evidence

This operational patch makes projection bytes independent of the checkout
transport spelling. The projector verifies the observed GitHub repository
identity, then records the configured canonical HTTPS URL in the release.

The read-only post-write check caught the non-canonical Actions checkout URL
before deployment. No Vercel hook fired. The corrected projection produces the
same bytes from local HTTPS, GitHub Actions HTTPS, or an allowed SSH checkout.

No protocol, authority, canonical frontier, editorial, or accepted-state byte
changes in this patch.
