# Vela Web v0.420.5

Vela Web 0.420.5 repairs the real-reader release gate after the current Erdős
Frontier advanced from 2,191 to 2,192 canonical events.

The production projection was already correct and root-bound at the current
Erdős commit. The stale test expectation was the defect: it passed when the
SELECT-only production reader was absent, then correctly failed during the
editorial deployment that exercised the live reader.

This release:

- binds the regression to the exact 2,192-event Frontier state;
- keeps the `v0.420.4` Profile v1 disposable-rematerialization repair;
- deploys the unchanged editorial and Observatory products under one exact Web
  release identity; and
- records deletion of the obsolete `v0-370-read-model` Neon branch.

It changes no Frontier, projection root, protocol, authority, or accepted
scientific state.
