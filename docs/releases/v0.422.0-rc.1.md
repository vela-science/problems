# Vela Web v0.422.0-rc.1

Vela Web 0.422.0-rc.1 is the Observatory candidate for the current repository
epoch and Claim, Submission, Verification, and Decision vocabulary.

The candidate:

- reads the complete normalized `observatory.v4` projection through
  `vela.observatory-release-manifest.v5`;
- pins Vela `0.940.5` and its exact released generator binary;
- binds the current exact Git heads, repository roots, authority keysets, and
  authority policies of the Erdős, Sidon, Formal Conjectures, and Quantum Codes
  frontiers;
- resolves current `vcl_` Claim routes to their immutable imported `vf_` graph
  identities without conflating the two object families;
- renders the rejected Erdős bounded-search Proposal, its passing Verification
  Record, and its terminal Decision as distinct records;
- preserves the complete Erdős graph of 4,668 nodes and 5,035 edges;
- keeps Neon a disposable, SELECT-only read projection with an atomic
  release-root pointer; and
- removes the retired publication-facts builder from current `main`, while
  preserving its exact Phase 1 evidence at `v0.420.2`.

The verified inactive projection candidate is:

```text
sha256:5e34b24d33ed7034e2872f5731d7380afcc5ce2c0adea2fb833b8f98b557b42c
```

Activation is a separate explicit root-bound operation. This candidate changes
no canonical Frontier byte, accepted Claim Standing, repository authority
rule, or editorial route.
