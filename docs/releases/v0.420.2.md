# Vela Web v0.420.2

This patch advances the read-only Observatory projector to the exact Vela
`v0.914.1` maintenance release and adds the second bounded Erdős 1056 run to
the rooted run ledger.

It changes no route, UI component, authority operation, protocol contract, or
editorial surface. The canonical frontier repositories remain the source of
truth; Neon remains a disposable normalized projection.

The refresh must preserve every scientific root while:

- verifying the released Linux binary and archive hashes;
- importing the current exact frontier commits;
- retaining both bounded Erdős runs as pending Defer evidence;
- activating one atomic projection release; and
- redeploying only the Observatory against that rooted release.
