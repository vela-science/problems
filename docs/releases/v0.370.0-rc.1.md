# Observatory v0.370.0-rc.1 release evidence

This candidate replaces the sampled whole-frontier projection with a complete,
normalized, release-scoped read model while leaving canonical Git repositories
authoritative.

- Erdős imports 4,668 graph nodes, 5,035 edges, 1,217 problem nodes, 2,764
  graph findings, and 2,770 complete finding records.
- The rooted graph source is
  `sha256:c332833c4bea08f2d31ada11953e386095a9f77f50010c29e228cafe562c0b20`;
  its canonical frontier root is
  `sha256:1faedc24f040a60a22177b456c74b969a61ce8836082297b1835797a57b4fa56`.
- Candidate releases insert and verify before an atomic current-pointer change.
  A deliberately invalid candidate failed verification without moving that
  pointer.
- Vercel reads through the native `observatory_projection_reader` role. Tests
  prove it inherits no managed superuser role, cannot write, and cannot select
  outside the `observatory` schema.
- Problems now inherit the persistent frontier shell. The complete Sigma map,
  its keyboard-accessible ledger, normalized search, record routes, and exact
  APIs are all bound to one retained release root.
- The measured in-app Browser matrix passed 66 route-and-viewport combinations
  at 320, 375, 414, 768, 1280, and 1440 pixels with no overflow, missing route
  heading, or runtime error. The first-party Impeccable score is 97/100 and the
  deterministic detector returned no advisory.
- The build emits 42 bounded prebuilt route artifacts. The complete graph is
  370,747 bytes gzip and the audited search response is 6,522 bytes gzip.

No protocol, canonical frontier, accepted-state, authority, signing, editorial,
or Fly deployment change is included. Legacy projection storage remains intact
through the required 72-hour rollback window.
