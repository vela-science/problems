# Observatory v0.360.1 release evidence

This operational patch makes the canonical Git-to-Neon projection refresh
deterministic and completes the Vercel handoff.

- The refresh workflow pins the projection indexer to Vela commit
  `995851fd1c3580fa946a514b11f18bc94a4038b6`.
- Frontier inspection uses the published Vela `0.912.0` Linux binary after
  verifying the release archive SHA-256.
- The Rust projection build is cached and does not rebuild the Vela CLI.
- A successful source replay, Neon write, and read-only corpus verification
  triggers the Observatory Vercel deployment hook.
- A failed refresh cannot trigger deployment.

No protocol, authority, canonical frontier, editorial, or accepted-state byte
changes in this patch.
